<div data-role="content" data-inset="true">
	<%= form_for(@item) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<p>Price (USD)</p>
	<%= f.text_field :price %>
	<%= f.text_field :lat, :id => "lat" %>
	<%= f.text_field :lng, :id => "lng" %>
	
	<%= f.submit "Create", "data-role" => "button", "data-inline" => "true", "data-theme" => "b" %>
	<% end %>

</div>
<div id="container"></div>

