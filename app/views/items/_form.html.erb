<div data-role="content" data-inset="true">
	<%= form_for(@item) do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>
	<p>Price (USD)</p>
	<%= f.text_field :price %>
	<%= f.hidden_field :address, :value=>"a" %>
	<%= f.text_field :latitude, :id => "lat", :precision=>10, :scale=>6 %>
	<%= f.text_field :longitude, :id => "lng", :precision=>10, :scale=>6 %>
	
	<%= f.submit "Create", "data-role" => "button", "data-inline" => "true", "data-theme" => "b" %>
	<% end %>

</div>
<div id="container"></div>

